#include "../../include/incs.h"

/*
KeyListener::KeyListener() {

}

static KeyListener KeyListener::getKeyListener() {
    if (KeyListener::instance == nullptr) {
        KeyListener keyListener = KeyListener();
        KeyListener::instance = &keyListener;
    }
    return KeyListener::instance;
}

static void KeyListener::keyCallback(GLFWwindow *glfwWindow, int key, int scancode, int action, int mods) {
    if (action == GLFW_PRESS) {
        KeyListener::keyPressed[key] = true;
    } else if (action == GLFW_RELEASE) {
        KeyListener::keyPressed[key] = false;
    }
}

bool KeyListener::isKeyPressed(int keyCode) {
    return KeyListener::keyPressed[keyCode];
}
*/